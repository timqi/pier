// The numbers more than one area has to agree on, in a leaf every area and the
// browser may import: a wrong copy makes two surfaces disagree about one session.

/** How much of a message becomes a title, wherever one is derived (agent/, web/). */
export const SESSION_TITLE_MAX = 80;
